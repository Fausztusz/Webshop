middleware/product:
GET + POST/product add -                    ||
GET/product - list                          ||  CRD
GET/product delete/:id -                    ||

middleware/cart:
GET + POST/cart/add                         ||
GET/cart/delete/:id                         ||
GET/cart/modify/:id                         ||  CRUD
GET/cart -list                              ||
GET/cart/buy ->finish shopping->redirect    ||

middleware/users:
GET/users -list                             ||  RD
GET/users/delete/:id                        ||

middleware/generic:
GET/login                                   ||  Handle auth
GET/logout                                  ||
GET/- redirectToMainPage                    ||